<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neon.niloadmin.mapper.UserCommentActionMapper">
    <!--实体映射-->
    <resultMap id="base_result_map" type="com.neon.nilocommon.entity.po.UserCommentAction">
        <!--自增ID-->
        <id column="action_id" property="actionId"/>
        <!--视频ID-->
        <result column="video_id" property="videoId"/>
        <!--评论ID-->
        <result column="comment_id" property="commentId"/>
        <!--0:评论点赞 1:评论点踩-->
        <result column="action_type" property="actionType"/>
        <!--用户ID-->
        <result column="user_id" property="userId"/>
        <!--操作时间-->
        <result column="action_time" property="actionTime"/>
    </resultMap>

    <!-- 简短的映射，索引覆盖 -->
    <resultMap id="brief_result_map" type="com.neon.nilocommon.entity.po.UserCommentAction">
        <!--自增ID-->
        <id column="action_id" property="actionId"/>
        <!--评论ID-->
        <result column="comment_id" property="commentId"/>
        <!--0:评论点赞 1:评论点踩-->
        <result column="action_type" property="actionType"/>
        <!--用户ID-->
        <result column="user_id" property="userId"/>
    </resultMap>

    <!-- 通用查询结果列-->
    <sql id="base_column_list">
        u.action_id,u.video_id,u.comment_id,u.action_type,u.user_id,
		 u.action_time
    </sql>

    <sql id="brief_column_list">
        u.action_id,u.comment_id,u.action_type,u.user_id
    </sql>

    <sql id="base_condition_filed">
        <if test="query.actionId != null">
            and u.action_id = #{query.actionId}
        </if>
        <if test="query.videoId != null">
            and u.video_id = #{query.videoId}
        </if>
        <if test="query.commentId != null">
            and u.comment_id = #{query.commentId}
        </if>
        <if test="query.actionType != null">
            and u.action_type = #{query.actionType}
        </if>
        <if test="query.userId != null">
            and u.user_id = #{query.userId}
        </if>
        <if test="query.actionTime != null and query.actionTime!=''">
            <![CDATA[ and  u.action_time=str_to_date(#{query.actionTime}, '%Y-%m-%d') ]]>
        </if>
    </sql>

    <!-- 通用查询条件列-->
    <sql id="query_condition">
        <where>
            <include refid="base_condition_filed"/>
            <if test="query.actionTimeStart!= null and query.actionTimeStart!=''">
                <![CDATA[ and  u.action_time>=str_to_date(#{query.actionTimeStart}, '%Y-%m-%d') ]]>
            </if>
            <if test="query.actionTimeEnd!= null and query.actionTimeEnd!=''">
                <![CDATA[ and  u.action_time< date_sub(str_to_date(#{query.actionTimeEnd},'%Y-%m-%d'),interval -1 day) ]]>
            </if>
        </where>
    </sql>

    <!-- 查询集合-->
    <select id="selectList" resultMap="base_result_map">
        SELECT
        <include refid="base_column_list"/>
        FROM user_comment_action u
        <include refid="query_condition"/>
        <if test="query.orderBy!=null">
            order by ${query.orderBy}
        </if>
        <if test="query.pageCalculator!=null">
            limit #{query.pageCalculator.start},#{query.pageCalculator.size}
        </if>
    </select>

    <!-- 查询数量-->
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT count(1) FROM user_comment_action u
        <include refid="query_condition"/>
    </select>

    <!-- 插入 （匹配有值的字段）-->
    <insert id="insert" parameterType="com.neon.nilocommon.entity.po.UserCommentAction">
        <selectKey keyProperty="bean.actionId" resultType="Long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user_comment_action
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.videoId != null">
                video_id,
            </if>
            <if test="bean.commentId != null">
                comment_id,
            </if>
            <if test="bean.actionType != null">
                action_type,
            </if>
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.actionTime != null">
                action_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.videoId!=null">
                #{bean.videoId},
            </if>
            <if test="bean.commentId!=null">
                #{bean.commentId},
            </if>
            <if test="bean.actionType!=null">
                #{bean.actionType},
            </if>
            <if test="bean.userId!=null">
                #{bean.userId},
            </if>
            <if test="bean.actionTime!=null">
                #{bean.actionTime},
            </if>
        </trim>
    </insert>

    <!-- 插入或者更新 （匹配有值的字段）-->
    <insert id="insertOrUpdate" parameterType="com.neon.nilocommon.entity.po.UserCommentAction">
        INSERT INTO user_comment_action
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bean.actionId != null">
                action_id,
            </if>
            <if test="bean.videoId != null">
                video_id,
            </if>
            <if test="bean.commentId != null">
                comment_id,
            </if>
            <if test="bean.actionType != null">
                action_type,
            </if>
            <if test="bean.userId != null">
                user_id,
            </if>
            <if test="bean.actionTime != null">
                action_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="bean.actionId!=null">
                #{bean.actionId},
            </if>
            <if test="bean.videoId!=null">
                #{bean.videoId},
            </if>
            <if test="bean.commentId!=null">
                #{bean.commentId},
            </if>
            <if test="bean.actionType!=null">
                #{bean.actionType},
            </if>
            <if test="bean.userId!=null">
                #{bean.userId},
            </if>
            <if test="bean.actionTime!=null">
                #{bean.actionTime},
            </if>
        </trim>
        on DUPLICATE key update
        <trim prefix="" suffix="" suffixOverrides=",">
            <if test="bean.videoId!=null">
                video_id = VALUES(video_id),
            </if>
            <if test="bean.commentId!=null">
                comment_id = VALUES(comment_id),
            </if>
            <if test="bean.actionType!=null">
                action_type = VALUES(action_type),
            </if>
            <if test="bean.userId!=null">
                user_id = VALUES(user_id),
            </if>
            <if test="bean.actionTime!=null">
                action_time = VALUES(action_time),
            </if>
        </trim>
    </insert>

    <!-- 添加 （批量插入）-->
    <insert id="insertBatch" parameterType="com.neon.nilocommon.entity.po.UserCommentAction" useGeneratedKeys="true"
            keyProperty="actionId">
        INSERT INTO user_comment_action(
        video_id,
        comment_id,
        action_type,
        user_id,
        action_time
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.videoId},
            #{item.commentId},
            #{item.actionType},
            #{item.userId},
            #{item.actionTime}
            )
        </foreach>
    </insert>

    <!-- 批量新增修改 （批量插入）-->
    <insert id="insertOrUpdateBatch" parameterType="com.neon.nilocommon.entity.po.UserCommentAction">
        INSERT INTO user_comment_action(
        video_id,
        comment_id,
        action_type,
        user_id,
        action_time
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.videoId},
            #{item.commentId},
            #{item.actionType},
            #{item.userId},
            #{item.actionTime}
            )
        </foreach>
        on DUPLICATE key update
        video_id = VALUES(video_id),
        comment_id = VALUES(comment_id),
        action_type = VALUES(action_type),
        user_id = VALUES(user_id),
        action_time = VALUES(action_time)
    </insert>

    <!--多条件修改-->
    <update id="updateByParam" parameterType="com.neon.nilocommon.entity.query.UserCommentActionQuery">
        UPDATE user_comment_action u
        <set>
            <if test="bean.videoId != null">
                video_id = #{bean.videoId},
            </if>
            <if test="bean.commentId != null">
                comment_id = #{bean.commentId},
            </if>
            <if test="bean.actionType != null">
                action_type = #{bean.actionType},
            </if>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.actionTime != null">
                action_time = #{bean.actionTime},
            </if>
        </set>
        <include refid="query_condition"/>
    </update>

    <!--多条件删除-->
    <delete id="deleteByParam">
        delete u from user_comment_action u
        <include refid="query_condition"/>
    </delete>

    <!-- 根据ActionId修改-->
    <update id="updateByActionId" parameterType="com.neon.nilocommon.entity.po.UserCommentAction">
        UPDATE user_comment_action
        <set>
            <if test="bean.videoId != null">
                video_id = #{bean.videoId},
            </if>
            <if test="bean.commentId != null">
                comment_id = #{bean.commentId},
            </if>
            <if test="bean.actionType != null">
                action_type = #{bean.actionType},
            </if>
            <if test="bean.userId != null">
                user_id = #{bean.userId},
            </if>
            <if test="bean.actionTime != null">
                action_time = #{bean.actionTime},
            </if>
        </set>
        where action_id=#{actionId}
    </update>

    <!-- 根据ActionId删除-->
    <delete id="deleteByActionId">
        delete
        from user_comment_action
        where action_id = #{actionId}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByActionId" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from user_comment_action u where action_id=#{actionId}
    </select>

    <!-- 根据CommentIdAndUserIdAndActionType修改-->
    <update id="updateByCommentIdAndUserIdAndActionType"
            parameterType="com.neon.nilocommon.entity.po.UserCommentAction">
        UPDATE user_comment_action
        <set>
            <if test="bean.videoId != null">
                video_id = #{bean.videoId},
            </if>
            <if test="bean.actionTime != null">
                action_time = #{bean.actionTime},
            </if>
        </set>
        where comment_id=#{commentId} and user_id=#{userId} and action_type=#{actionType}
    </update>

    <!-- 根据CommentIdAndUserIdAndActionType删除-->
    <delete id="deleteByCommentIdAndUserIdAndActionType">
        delete
        from user_comment_action
        where comment_id = #{commentId}
          and user_id = #{userId}
          and action_type = #{actionType}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByCommentIdAndUserIdAndActionType" resultMap="base_result_map">
        select
        <include refid="base_column_list"/>
        from user_comment_action u where comment_id=#{commentId} and user_id=#{userId} and action_type=#{actionType}
    </select>

    <select id="selectByCommentIdAndUserId" resultMap="brief_result_map">
        SELECT
        <include refid="brief_column_list"/>
        FROM user_comment_action
        <where>
            comment_id = #{commentId} and user_id = #{userId}
        </where>
    </select>
</mapper>